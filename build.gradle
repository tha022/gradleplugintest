// import org.gradle.GreetingTask;

group 'com.seniordevonly'
version '1.1'

apply plugin: 'groovy'

//mainClassName = "org.gradle.GreetingTask"

repositories {
    mavenCentral()
}

/*buildscript {
    repositories {
        maven {
            url uri('build/libs')
        }
    }
    dependencies {
        classpath group: 'com.seniordevonly', name: 'gradleplugin',
                version: '1.0-SNAPSHOT'
    }
}*/

dependencies {
    compile localGroovy()
    compile gradleApi()

    //compeil 'com.android.tools.build:gradle:1.2.3'
    compile 'com.android.tools.build:gradle:1.2.3'


    //compile 'com.squareup.spoon:spoon-runner:1.1.9'
    //compile 'de.felixschulze.teamcity:teamcity-status-message-helper:1.1'


    //compile 'org.codehaus.groovy:groovy-all:2.4.3'
    //testCompile group: 'junit', name: 'junit', version: '4.11'
}

task hello(type: GreetingTask)

// Customize the greeting
task greeting(type: GreetingTask) {
    greeting = 'greetings from GreetingTask'
}


